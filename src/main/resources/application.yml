spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST}:5432/${DB_NAME}
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create
    show-sql: true

server:
  port: 8080


paystack:
  secret:
    key: Bearer sk_test_e6e5c33035a78bc0c1c743c9d26588b346fc085b
#  payment:
#    channels: card, bank, ussd, qr, mobile_money, bank_transfer, eft
  base:
    url: "https://api.paystack.co"
  callback:
    url: "https://5d6e-105-112-176-203.ngrok-free.app/verify-transaction"


